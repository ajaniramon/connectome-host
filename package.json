{
  "name": "@animalabs/connectome-host",
  "version": "0.8.0",
  "description": "General-purpose agent TUI host with recipe-based configuration",
  "type": "module",
  "scripts": {
    "start": "bun src/index.ts",
    "dev": "bun --watch src/index.ts",
    "test": "bun test",
    "version": "bun scripts/release-changelog.ts && git add CHANGELOG.md changelog.d",
    "build:web": "npm --prefix web install && npm --prefix web run build",
    "build:web:ci": "npm ci --prefix web && npm --prefix web run build",
    "relock:web": "rm -rf web/node_modules web/package-lock.json && npm --prefix web install",
    "postinstall": "test -d web && npm --prefix web install && npm --prefix web run build || true"
  },
  "dependencies": {
    "@animalabs/agent-framework": "^0.13.0",
    "@animalabs/chronicle": "^0.3.0",
    "@animalabs/context-manager": "^0.8.0",
    "@animalabs/membrane": "git+https://github.com/antra-tess/membrane.git#28ea9ffde04758963742e76ad2338592df59e463",
    "@opentui/core": "^0.1.82"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/anima-research/connectome-host.git"
  },
  "homepage": "https://github.com/anima-research/connectome-host#readme",
  "bugs": {
    "url": "https://github.com/anima-research/connectome-host/issues"
  },
  "overrides": {
    "@animalabs/membrane": "git+https://github.com/antra-tess/membrane.git#28ea9ffde04758963742e76ad2338592df59e463"
  }
}
